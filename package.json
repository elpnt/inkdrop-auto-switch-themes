{
  "name": "auto-switch-themes",
  "main": "./lib/index",
  "version": "1.1.0",
  "description": "Switch the themes automatically based on the system light/dark mode",
  "keywords": [],
  "repository": "https://github.com/elpnt/inkdrop-auto-switch-themes",
  "license": "MIT",
  "engines": {
    "inkdrop": "^5.x"
  },
  "scripts": {
    "build": "swc src -d lib",
    "watch": "swc -w src -d lib"
  },
  "dependencies": {},
  "devDependencies": {
    "@swc/cli": "^0.1.57",
    "@swc/core": "^1.3.24",
    "chokidar": "^3.5.3"
  }
}
